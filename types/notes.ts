export type VisaHistory = {
    type: string;
    grantDate: string;
    expiryDate: string;
    id?: string;
  };
  
export type CoEHistory = {
    course: string;
    institution: string;
    startDate: string;
    endDate: string;
    id?: string;
  };
  